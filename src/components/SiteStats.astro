---
interface Props {
  postCount:  number;
  totalWords: number;
  tagCount:   number;
  daysOnline: number;
}

const { postCount, totalWords, tagCount, daysOnline } = Astro.props;

const stats = [
  { value: postCount  || '—',                    label: 'Posts'       },
  { value: totalWords ? `${totalWords}k` : '—', label: 'Words'       },
  { value: tagCount   || '—',                    label: 'Tags'        },
  { value: daysOnline || '—',                    label: 'Days Online' },
];
---

<section
  class="w-full bg-[#f7f6f2]"
  aria-label="Site statistics"
>
  <!-- Centered section label -->
  <div class="max-w-[1100px] mx-auto px-6 pt-10">
    <div class="flex items-center gap-4 mb-6">
      <div class="flex-1 h-px" style="background:linear-gradient(to right,transparent,rgba(57,197,187,0.4))"></div>
      <span class="text-[10px] font-semibold tracking-[0.22em] uppercase" style="color:#39c5bb">Site Stats</span>
      <div class="flex-1 h-px" style="background:linear-gradient(to left,transparent,rgba(57,197,187,0.4))"></div>
    </div>
  </div>

  <!-- Stat cards -->
  <div class="max-w-[1100px] mx-auto px-6 pb-12">
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-4">
      {stats.map((s) => (
        <div class="flex flex-col items-center gap-1.5 py-6 rounded-2xl bg-white border border-[#ece9e0] shadow-[0_4px_20px_rgba(36,35,31,0.03)]">
          <span
            class="font-medium tabular-nums leading-none"
            style="font-size: clamp(1.85rem, 3.5vw, 2.4rem); color:#24231f;"
          >
            {s.value}
          </span>
          <span
            class="text-[10px] font-medium uppercase tracking-[0.18em]"
            style="color:#787670;"
          >
            {s.label}
          </span>
        </div>
      ))}
    </div>
  </div>
</section>
